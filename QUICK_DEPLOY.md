# Быстрый деплой исправления дубликатов событий

## 🚀 ОДНА КОМАНДА (Самый простой способ)

На вашем локальном компьютере:

```bash
cd /Users/bebdyshev/Documents/Github/lms/backend
./deploy_to_production.sh
```

Скрипт спросит адрес сервера, скопирует файлы и покажет команды для перезапуска.

---

## ✅ РУЧНОЙ ДЕПЛОЙ (если нужен контроль)

### На вашем локальном компьютере:

```bash
cd /Users/bebdyshev/Documents/Github/lms/backend

# Скопировать исправленные файлы на сервер
# ВАЖНО: Замените "server" на ваш IP или hostname
scp src/services/event_service.py root@server:/root/projects/lms/src/services/
scp src/routes/events.py root@server:/root/projects/lms/src/routes/
```

### На сервере:

```bash
cd /root/projects/lms

# Перезапустить backend
docker-compose restart web

# Проверить что запустилось без ошибок
docker-compose logs web | tail -30
```

**Готово!** Дубликаты в календаре должны исчезнуть.

---

## Альтернатива: Hotfix скрипт (если нужно применить патчи)

На вашем локальном компьютере:

```bash
cd /Users/bebdyshev/Documents/Github/lms/backend

# Скопировать hotfix скрипт на сервер
scp hotfix_deduplication.py root@server:/root/projects/lms/
```

На сервере:

```bash
cd /root/projects/lms

# Запустить hotfix (применит патчи к существующим файлам)
python3 hotfix_deduplication.py

# Перезапустить
docker-compose restart web
```

---

## Проверка после деплоя

### В браузере:
- Откройте календарь на вашем LMS сайте
- Убедитесь что события **не дублируются**
- Каждый таймслот должен показываться **один раз**
- Название: `"Группа: Class N"` вместо дублей

### На сервере:
```bash
# Проверить что нет ошибок Python
docker-compose logs web | grep -i "error\|exception" | tail -20

# Проверить статус контейнера
docker-compose ps
# Должен быть "Up" или "healthy"
```

---

## Откат (если что-то пошло не так)

На сервере уже создан автоматический бэкап:

```bash
cd /root/projects/lms

# Посмотреть доступные бэкапы
ls -la backups/

# Восстановить из последнего бэкапа
BACKUP=backups/deduplication_fix_20260202_060240  # Ваш timestamp
cp $BACKUP/event_service.py src/services/
cp $BACKUP/events.py src/routes/

# Перезапустить
docker-compose restart web
```

---

## Почему автоматические скрипты не сработали?

Скрипты `deploy_deduplication_fix.py` ищут специфичные паттерны кода для применения патчей, но:
- Файлы на сервере могут отличаться от ожидаемых
- Уже частично исправлены
- Имеют другое форматирование

**Решение**: Скопировать целиком исправленные файлы с локального компьютера → проще и надёжнее.

---

## Что исправляет этот деплой?

✅ Виртуальные события получают `_group_ids` атрибут  
✅ Дедупликация использует `_group_ids` вместо `event_groups`  
✅ Календарь не показывает дубликаты "Planned" и "Online Class"  
✅ События правильно фильтруются по группам  

**Исправление на корню** - решает проблему в EventService, а не маскирует симптомы на фронтенде.

